# Planned Test Layout

The test tree is reserved for future implementation phases.

Expected areas:

- `unit/`: small isolated tests for loaders, judges, and utility logic
- `integration/`: Neovim session and end-to-end gameplay tests
- `schema/`: validation tests for authored YAML content
- `fixtures/`: test data shared by multiple suites

Phase 1 keeps these directories as placeholders only.
