# Prototypes

This directory is reserved for experiments, throwaway UI sketches, or temporary authoring prototypes.

Anything stored here should be treated as non-production material unless it is later promoted into `docs/`, `data/`, or `src/`.
